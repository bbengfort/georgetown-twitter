# Copy and paste this into "conf/chirpy.yaml" and then fill in the values
# below correctly for your configuration.

debug: True
testing: False
api_key: "[INSERT API KEY HERE]"
api_secret: "[INSERT API SECRET KEY HERE]"
fixtures: "path/to/download/files.json"
mongo:
    host: localhost
    port: 27017
    database: tweets
